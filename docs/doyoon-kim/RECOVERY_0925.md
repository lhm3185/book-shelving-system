# 9/25 복구 절차 — **순서를 지킨다. 중간에 고치지 않는다**

웹 클로드 v41 회신 §1 그대로. 2026-09-24 밤 작성.

> **오늘 하루를 잃은 이유는 1번을 건너뛴 것이다.** 30분이면 되는 단계였다.

---

## 0. 목표 — **이 둘만 되면 끝이다** (도윤님 확정, 9/24 밤)

```
① 비전팀이 구현한 스캔 동작을 선반 앞에서 실행
   → 빈칸 좌표 받기 → 책 파지 → 그 좌표에 꽂기
   **3번째·4번째 선반에 빈칸 하나씩** 있으니 **두 권** 넣고 홈 복귀
② 로봇팔 동작을 가능한 한 **arm_kinematics 에서 호출하는 방식**으로 구현
```

**그 다음 업무는 없다.** 완주율 표본·산출물 문서·다각도 녹화는 전부 뒤로 간다.

> ~~`arm_kinematics` 전환은 하지 않는다~~ — **취소.** 웹 클로드 회신을 받고 내가
> 그렇게 정했는데, 도윤님이 ②를 목표 둘 중 하나로 두셨다. 도윤님 결정이 우선이다.

**그리고 "2권째는 자리가 없다" 는 내가 틀렸다.** 내가 센 것은 **한 판 안에 두 권**
이었다. 판마다 한 권씩이면 둘 다 넉넉하다:

```
thirdFloor  빈칸 96.0 mm  여유 +30.4 mm
forthFloor  빈칸 59.9 mm  여유 +12.4 mm
```

## 0.1 업무 방식 (도윤님 확정)

```
시뮬은 **항상 rqt 와 함께** — 도윤님이 자리에서 같이 확인하신다
그만큼 백그라운드에서 코드·시뮬 검증을 충분히 하고
띄울 때는 **최종확인**
```

**판 하나가 도윤님 시간을 쓴다.** 판을 요청하기 전에 셋에 답한다:

```
이 판으로 무엇을 확인하는가        한 문장
코드·로그로는 왜 답이 안 되는가    답이 되면 요청하지 않는다
실패하면 무엇을 알게 되는가        "그냥 안 되네" 면 요청 가치가 없다
```

---

## 1. 기준선 — `04441f6` 한 판, **아무것도 고치지 않고**  [30분]

**`demo_check.sh` 를 쓰면 안 된다.** 그 커밋에는 없다(오늘 만들었다). 그리고 우리
스크립트로 띄우는 것 자체가 이미 변경이다. **어제 쓰던 스크립트가 그 커밋 안에 있다:**

```bash
git stash list                       # 남은 게 없는지
git checkout 04441f6
bash scripts/demo/full_cycle.sh      # ← 이것이 "어제 그대로" 다
```

그 스크립트가 무엇을 하는지 읽어 뒀다. **오늘 우리가 다시 만든 것을 이미 다 하고 있다:**

```
Isaac + 비전 + 조작 + **주행 + FSM(task_manager) + 반납기**   ← 우리 run_vision.sh 는 FSM 이 없다
rqt_image_view 두 개 (/perception/debug_image · depth)         ← demo_check.sh 가 다시 만든 것
logs/rec 에 뷰포트 PNG 녹화 — 주석: "**스캔 모션을 눈으로 확인한다**"
```

**우리와 다른 것 — 첫 판에서 바꾸지 말 것:**

```
ROS_DOMAIN_ID   **129**  (우리 run_vision.sh 는 130)
SIM_HOME_Q      **없다** ← 이것이 오늘 사달의 원인이다. 어제는 yaml 홈(2.811)으로 돌았다
그 밖           SIM_HOME_J7_DEG=90 · SIM_HOME_SHIFT=0,0,0.10 · SIM_FIX_BASE=0
                SIM_GRIP_ROT90=1 · SIM_GRASP_KINEMATIC=1 · SIM_TRAY_TO=4.907,-5.782,0.3365
레벨            /home/rokey/env_v5/Collected_ing_library_env_v5/ing_library_env_v5.usd
```

> **주의**: 그 스크립트는 맨 앞에서 `pkill -f "install/shelving_…"` 를 돈다.
> 데스크탑에 남의 프로세스가 없을 때만 돌린다. 지금은 정리돼 있으니 괜찮다.

### 1.1 **이 PC 에서는 그대로 안 돈다** — 세 군데가 교육장 PC 전용이다

```
32줄  LEVEL     /home/rokey/env_v5/…            없음  → SIM_LEVEL 로 넘긴다
35줄  BOOKS     /home/rokey/b1_work/…           없음  → SIM_BOOKS 로 넘긴다
                (같은 파일이 $REPO/simulation/assets/book_dataset/usd_v2/ 에 있다)
43줄  hostname  == "IsaacSim15"                 아님  → 호스트명을 **더한다** (지우지 않는다)
```

셋 다 **44줄 위**, 즉 Isaac 이 뜨기도 전이다. **44줄 아래는 한 글자도 안 건드린다** —
거기부터가 동작이다. "이 스크립트가 어느 PC 에서 도는가" 와 "로봇이 무엇을 하는가"
는 다른 것이고, 앞의 것만 맞춘다.

### 1.2 **그래서 이 판은 "어제 재현" 이 아니다**

```
어제 되던 것   교육장 GPU PC(IsaacSim15) · 9/23 레벨
이 판          집 데스크탑(BryanKUBT) · 오늘 레벨(Final_Level_Library)
```

**이 PC 에서 이 코드를 돌려 본 적이 없다.** 그러니 실패해도 **우리 오늘 변경 탓이라고
단정할 수 없다** — PC 와 레벨이라는 변수가 이미 둘 있다.

> 오늘 내가 여러 번 한 실수가 **변수가 둘인데 하나로 단정한 것**이다. 그래서
> 미리 적는다. 이 판의 결과는 **"지금 여기서 이 코드가 어디까지 가는가"** 이지
> "어제가 무엇이었나" 가 아니다.

레벨은 `env.txt` 로 확정했다 — 오늘 비전 병합 이후 판 118개가 전부
`Final_Level_Library.usdc` 다. **같은 레벨에 어제 코드**가 이 판의 조건이다.

**여기서 코드를 고치지 않는다.** 안 되면 안 되는 대로 기록한다 — "어제 되던 것"
이라는 전제 자체가 틀렸다는 뜻이고, 그것도 알아야 할 사실이다.

되면:

```bash
git tag baseline-vision-0923 04441f6
```

그리고 **영상과 로그를 남긴다.** 기준선은 숫자가 아니라 **영상과 커밋 해시**다.
오늘 이걸 잡은 것은 판정 도구가 아니라 도윤님이 화면을 본 것이었다.

```
남길 것   판 디렉터리 통째 · 화면 녹화 또는 시뮬 녹화 한 벌
          [스윕] 줄 전부 · 시작 자세 q · 사이클 결과
```

---

## 2. 우리 브랜치, **오늘 바꾼 것을 전부 끈 상태**로 한 판  [30분]

```bash
git checkout work/vision_merge_0924
```

끌 것 — **기준선이 안 읽던 것은 전부 끈다.** `04441f6` 을 확인해 보니
콜리전 교정 스위치 셋을 **읽는 코드 자체가 없었다**(`grep` 0건). 즉 어제는
런타임 교정이 없었다. 그러니 끄는 것이 "어제와 같게" 다.

```
SIM_HOME_Q               **빼고 돌린다** (yaml 홈으로) ← 가장 중요
slot_x_snap_to_gap       false
slot_y_from_shelf_front  false
SIM_BOOK_COLL            빈 값
SIM_SHELF_COLL           빈 값
SIM_SHELF_ROW_MEASURE    0
scan_command             건드리지 않는다 (이미 scan_sweep = 코드 기본)
```

> 데스크탑이 "레벨이 이미 교정됐으니 런타임 스위치는 무의미하지 않나" 고 물었다.
> 그 말이 맞을 수 있지만 **그건 4번에서 확인할 일**이다. 2번의 질문은
> "오늘 바꾼 것을 다 끄면 기준선과 같은가" 하나다. **같으면 무해가 증명되고,
> 다르면 그 차이가 범인이다.** 판 하나 더 도는 값은 그만큼 한다.
>
> 그리고 4번의 첫 항목이 콜리전 교정이다 — 바로 다음에 켜 보면 된다.

**1번과 같은가?** 같으면 우리 변경은 무해하고, 범인은 위 넷 안에 있다.
다르면 **그 차이 목록이 곧 범인 목록**이다.

> **5분 규칙**: 어제 되던 것이 오늘 안 되면 원인은 반드시 **오늘 바꾼 것** 안에
> 있다. 하나씩 끈다. 오늘 나는 스위치를 **끄는 데** 안 쓰고 **더 붙이는 데** 썼다.

---

## 3. 자세를 나눈다 — `q_scan` / `q_pick`  [1시간]

원인은 홈 자세 하나가 **두 가지 뜻을 겸한 것**이다.

```
yaml 홈      파지 여유 0.128 (작다)  ·  아래 판 스윕 5/5 (0.166)   ← 스캔에 좋다
SIM_HOME_Q   파지 여유 0.707 (크다)  ·  아래 판 스윕 3/5 (0.100)   ← 파지에 좋다
```

**하나를 고를 일이 아니다. 나눈다.**

```
q_scan   스캔 진입 자세 = yaml 홈 계열
q_pick   파지 진입 자세 = SIM_HOME_Q 계열
사이클:  … → moveJ(q_scan) → 스캔 → moveJ(q_pick) → 파지 → 삽입 → …
```

전이는 `moveJ` 한 번. 몇 초 더 걸리지만 **두 구간이 각자 최적 자세에서 돈다.**

**2번이 끝나기 전에는 쓰지 않는다.** 이것도 "확인 전에 고치기" 가 된다.

---

## 4. 오늘 얻은 것을 **하나씩** 얹는다  [반나절]

판마다 1번과 비교한다. 순서:

```
① 콜리전 교정      기울기 3.5° → 0.35° · 떠오름 18 mm → 0
② 빈칸 맞춤        검출 280 mm 흔들림 → 목표 0.2 mm
③ 그 밖
```

**배치 정확도 16판은 버린다.** 다른 동작 위에서 잰 숫자라 그대로 못 쓴다.
복구된 동작 위에서 다시 잰다 (9/26).

---

## 5. 이날부터 **모든 판이 자기 구성을 찍는다**

오늘 사고 넷 중 셋은 이것만 있었어도 첫 판에서 드러났다.

```
목표:      (한 문장 — 이 판으로 무엇을 확인하는가)
기준선:    (비교 대상 판 ID)
git:       (해시)      level: (md5)
q_home 실제값 · 환경변수 전체 · 프리셋 이름
```

**목표 문장과 기준선이 없으면 판을 돌리지 않는다.**

왜 이게 표류를 잡나: 동작을 바꾸면 **비교 대상이 더 이상 유효하지 않다.**
그러면 `기준선:` 을 고쳐야 하고, 고치는 것은 **사람이 내리는 명시적 결정**이 된다.
오늘은 그 결정이 아무 데서도 일어나지 않았다 — `scan_shelf` 로 갈아탄 뒤에도
앞 판들과 계속 비교하고 있었다.

> **표류는 도구로 안 잡히고 문장으로 잡힌다.**
> 오늘 도구를 7,584줄 붙였고, 원인을 찾은 것은 로그 한 줄이었다.

---

## 6. 막혔을 때

```
5분    오늘 바꾼 것을 하나씩 끈다
30분   그래도 안 되면 **막힌 지점을 숫자로** 보고하고 판단을 받는다
우회    불가피하면 **스위치 뒤에, 기본은 원래대로, 기준선 판을 함께 돌린다**
```

**우회인지 대안인지 가르는 한 문장**:
*이 변경 뒤에도 원래 질문에 답할 수 있는가.*
답할 수 없으면 우회다. `scan_shelf` 가 그랬다.

---

## 6.1 어제 스크립트에서 가져올 것 — **다시 만들지 않는다**

`scripts/demo/full_cycle.sh` 가 오늘 우리가 새로 만든 것을 이미 하고 있었다.
복구 뒤에도 **그 스크립트를 쓰는 쪽**이 맞다.

```
rqt 두 창            demo_check.sh 로 다시 만들 필요 없었다
뷰포트 녹화           record_views 로 다시 만들 필요 없었다
FSM 까지 포함한 전 구간  우리 run_vision.sh 는 FSM 없이 python 으로 몰았다
```

**오늘 만든 것 중 이 셋은 중복이었다.** 있는 것을 안 읽고 만들었다.

---

## 7. 9/25 저녁에 답해야 하는 것

> **지금 제출해야 한다면 무엇을 내는가.**

복구가 안 되면 9/26 에 계획을 바꾼다. 그때의 대안은 **`04441f6` 을 그대로 시연
기준으로 삼고**, 오늘 얻은 것은 문서로만 싣는 것이다. **그것도 완결된 산출물이다.**
