# 9/24 새벽 — 도윤님 판단이 필요한 것 셋

작성: 랩탑(집) 세션 · 측정: 데스크탑(GPU) 세션 · 밤샘 작업 중간 보고

---

## 먼저: 시연은 안전합니다

```
시연 조합 (환경변수)
  SIM_PICK_Y=-3.0695  SIM_GOAL_X=-0.3382
  SIM_CARRY_MODE=swing  SIM_RETURN_MODE=swing  SIM_RELEASE_OPEN_FIRST=1
  SIM_HOME_Q=-2.3634,-0.7300,-0.6933,-2.1884,-0.4396,1.5845,-0.5540

열다섯 판 이상 전부 합격 · min_margin 0.245~0.254 · 겹침 0 · 판정 code=0
도착 오차 안전 구간 -45 ~ +52 mm  (AMR 요구 정밀도 y ±30 mm 면 충분)
```

아래 셋은 **더 좋게 만드는** 이야기지 막힌 데가 아닙니다.

---

## 결정 1 — push 승인

데스크탑 세션이 커밋을 만들어 두고 **푸시를 못 하고 있습니다.** 브랜치는
`work/motion_api_0924` 이고 `main` 은 안 건드립니다.

특히 `cli_exchange/handoff_0924/ARRIVAL_TOLERANCE.md` 는 **AMR 담당에게 넘길
도착 정밀도 ±30 mm** 를 담고 있어 저장소 밖에 두면 안 됩니다.

> **여쭙는 것**: `work/motion_api_0924` 에 푸시해도 됩니까?

---

## 결정 2 — N5 (5권 연속 시연)

**지금 층(forthFloor)에서는 못 합니다.**

```
빈칸 두 곳뿐:
  59.9 mm  (좌우 여유 12.3 mm)   ← 지금 쓰는 칸. 기울기를 넣으면 한쪽 5.4 mm
  46.0 mm  (좌우 여유  5.4 mm)   ← 기울기 2.8° 면 한쪽이 이미 음수. 못 씁니다
```

실질적으로 **꽂을 수 있는 칸이 하나**입니다. 세 갈래입니다:

| | 내용 | 대가 |
|---|---|---|
| ㄱ | thirdFloor(z 1.042)를 같이 쓴다 | 팔 기준 z 가 전부 바뀌어 다시 재야 합니다 |
| ㄴ | 레벨에 빈칸을 더 낸다 | 레벨 수정 — 저희는 안 건드립니다 |
| ㄷ | 시연을 1~2권으로 줄인다 | 지금 검증된 범위 그대로, 추가 작업 0 |

> **여쭙는 것**: 어느 쪽으로 갈까요? (제 권고는 ㄷ → 시간이 남으면 ㄱ)

---

## 결정 3 — 콜리전 근사 교정 (새로 생긴 항목)

밤새 파고든 결과, **레벨의 콜리전 근사 두 곳이 문제를 만들고 있습니다.**
둘 다 **레벨 파일을 안 고치고** 런타임에 바로잡습니다.

### 3-1. 서가 — 책이 선반 판 위 18 mm 에 떠 있습니다

```
시각 판 윗면      0.4976   (메시 점 8개에서 직접 읽음)
책이 앉는 곳      0.5155   (네 판 전부 같은 자리)
```

서가 전체에 콜라이더가 **하나**뿐이고 근사가 `convexDecomposition` 입니다.
2.5 m 짜리 통짜 메시를 복셀로 근사하면 얇은 선반 판에 복셀 한 겹이 그대로
두께로 붙습니다 — 그 한 겹이 18 mm 입니다.

```bash
SIM_SHELF_COLL=none  SIM_SHELF_ROW_MEASURE=1
```

- 서가는 **정적**이라 삼각망 충돌이 합법입니다 → 콜라이더가 시각 형상과 같아집니다
- 판 높이를 레벨에서 재니 `0.355 × 1.4` 라는 출처 불명의 곱셈을 안 씁니다
- **경로를 한 점도 안 옮깁니다** (`min_margin` 0.246·0.248, 기준선 안)

**효과**: 떠오름 16 mm 가 사라지고 책이 판에 붙어 앉습니다. 지금은 시연 영상에서
한 권만 이웃보다 1.8 cm 높게 보입니다.

### 3-2. 책 — 꽂힌 책이 늘 2~3° 비뚤습니다 (검증 중)

레벨의 트레이 책 다섯 권이 전부 `convexHull` 입니다. 그런데 저희 코드에 이미
이런 주석이 있습니다(책을 직접 스폰할 때):

> 표지가 둥근 책은 convexHull 이면 흔들려 넘어진다. 책은 상자에 가까우므로
> **boundingCube** 로 두면 트레이에 그대로 서 있는다.

**그 처방을 스폰 책에만 하고 레벨 책에는 안 했습니다.**

```bash
SIM_BOOK_COLL=boundingCube
```

**근거**: 기울기를 못 움직이는 것을 일곱 가지 지웠습니다 — 도착 yaw, 파지 자세
정렬(여덟 판), 파지 시점 기울기, 손 안 책 자세, 떠오름 16 mm, 콜라이더 면 위치,
손가락 벌림. 아홉 판이 전부 2.08~3.51° 안입니다. **끌개가 있다**는 뜻이고,
둥근 표지의 볼록 껍질이 바로 그런 끌개입니다(밑면이 평평하지 않으면 어디서 놓든
같은 각도로 눕습니다).

방증: 레벨에 저장된 상태의 콜라이더 폭이 **35.2 mm — 규격과 정확히 같습니다.**
기울기는 시뮬이 돌면서 정착하며 생깁니다.

> **여쭙는 것**: 이 둘을 시연 조합에 넣을까요?
> 넣으면 다섯 판 확인 뒤 반영하고, 안 넣으면 스위치로만 남겨 둡니다.
> (지금도 시연은 됩니다. 얻는 것은 **예비**입니다 — 옆 여유가 한쪽 5.4 mm 인데
> 그중 3.9 mm 를 기울기가 먹고 있습니다.)

---

## 남은 것 (판단 불필요, 기록용)

- **Isaac 기동 크래시 7회** — 전부 `USD 열기` 직후, 재시도는 항상 한 번에 성공.
  `SIM_BOOT_GAP` 으로 피해지지 않는다는 것이 확인됐습니다(간격 둔 판에서도 발생).
  시연은 한 판만 띄우므로 실제 위험은 아닙니다. **시연 뒤 과제**로 남깁니다
- **책 프로파일** — 규격 폭 163.1 vs 실측 153~154 mm (10 mm 차이). 410 높이 검사가
  `slot_z + 폭/2` 로 재므로 5 mm 어긋납니다. 위 3번이 정리된 뒤 한 번에 고칩니다
- **`verify()["floor"]` 문턱 30 mm** — 18 mm 떠 있는 것을 못 잡았습니다. 3-1 이
  채택되면 같이 좁힙니다
- **비전팀 요청** (`TargetSlot.base_pose`) — 집에서는 연결이 안 됩니다.
  `docs/doyoon-kim/proposals/interfaces/` 에 적어 두었고 교육장 복귀 때 내보냅니다

---

## 오늘 밤의 패턴

발견한 결함 **아홉 개가 전부 같은 모양**이었습니다 — "레벨에서 잰 값" 을 상수로
박아 둔 자리들입니다.

```
트레이 출발점 · 꽂을 x · 스윕 기준점 · 406 기준점 · move_rig ·
손가락 벌림 축 · 파지 정렬 기준자세 · 선반 판 높이 · 콜리전 근사
```

매번 순수 모듈로 빼고 시험을 붙였습니다 (`tray_delivery.py`, `shelf_gap.py`,
`hand_drift.py`, `plan_inputs()`). 그래야 GPU 없이 확인됩니다.
