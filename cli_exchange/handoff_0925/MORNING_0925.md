# 아침 요약 — 2026-09-25 (밤샘 결과)

## 한 줄

```bash
bash scripts/demo/full_cycle.sh
```

**환경변수 없다.** 이 한 줄로 두 권 삽입 + 홈 복귀가 돈다. 한 판 약 11 분.
레벨·책 경로는 스크립트가 찾고, 트레이 좌표·운반 방식은 기본값에 들어갔다.

## 목표 ① 두 권 삽입 + 홈 복귀 — **5/5**

전부 위 명령 한 줄, 환경변수 없이 돌린 판이다.

| | 1권 (아래 판) | 2권 (위 판) | 겹침 | 복귀 | 완주 |
|---|---|---|---|---|---|
| D확인 | +2.1 mm · 0.24° | −1.4 mm · 0.52° | 없음 | (+4.986, −5.607) yaw +90.00° | ○ |
| B1 | +1.3 mm · 0.23° | −0.5 mm · 0.55° | 없음 | 같음 | ○ |
| B2 | +1.7 mm · 0.16° | −1.5 mm · 0.42° | 없음 | 같음 | ○ |
| B3 | +2.4 mm · 0.18° | −0.7 mm · 0.52° | 없음 | 같음 | ○ |
| B4 | +1.1 mm · 0.25° | −0.8 mm · 0.49° | 없음 | 같음 | ○ |

흩어짐 1권 +1.1~+2.4 mm · 2권 −1.5~−0.5 mm · 기울기 0.16~0.55°.
**복귀는 다섯 판 다 좌표·각도가 소수점까지 같다.**

B4 는 정리 커밋(`26476c9` 손잡이 제거, `7d7a6c8` 대조 수정) 뒤에 돌린 판이다 —
그 커밋들이 동작을 안 건드린 것도 이걸로 확인됐다.

## 목표 ② arm_kinematics 호출 — 경로는 들어갔고 **기본은 아직 Lula**

```
모델 대조    0.00 mm                     두 풀이기가 같은 팔을 본다 (통과)
해 대조      approach · down · lift  129개   차이 0 · 다중도못 0    → 옮겨도 된다
             carry_rotate          1871개   Lula만 102 · **다중도못 5** · 오차 4.00 mm
```

**`carry_rotate` 한 단계에만 몰려 있다.** 지금 켜면 꽂기 정확도(1.1~2.4 mm)가 먹힌다.
`SIM_IK_AK` 기본은 `0`(Lula). 켜는 기준은 **다중도못 0 · 오차 1 mm 대** 두 가지다.

다음 수(오전에 도윤님 확인 뒤): 파지 계열부터 단계별 스위치 / 계획 덤프 오프라인 대조 /
`carry_rotate` 원인.

## 오늘 밤 고친 것

| 증상 | 원인 | 고침 |
|---|---|---|
| 모든 관절이 움직이며 책을 돌린다 | `carry_rotate` 가 이동과 90° 회전을 한 덩어리로 | 스윙 분해 (j1 만 도는 수평 호) |
| 축 처짐 · 트레이 충돌 · 403 | 위와 같음 | 위와 같음 |
| 위 판 401 | 경유점 `transfer`(경로 최고점)가 안 풀린다 | 바깥으로 뻗어(r 0.50) 손목 먼저 돌리고 HORIZ 로 |
| 놓는 순간 59 mm 낙하 | 칸 바닥이 계산값, 판이 하나뿐인 스칼라 | 실측 판 목록에 스냅 |
| 복귀 자리가 30 cm 어긋남 | 팔 베이스 자리를 루트 목표로 줬다 | 루트 출발 자리를 따로 싣는다 |
| 복귀 각도가 90° 틀어짐 | `home` 에 yaw 를 안 실었다 | `[x, y, yaw]` 계약대로 |
| 꽉 찬 판에 밀어 넣으러 감 | '판이 비었다' 관측이 실측 빈칸을 우회 | 정상 판별을 타고 없으면 410 |

## 막힌 길 (다시 파지 말 것 — 전부 코드 주석에 숫자와 함께 있다)

```
clear 0.15      c 걸음 0.126 → 0.168 로 악화
clear 0.60      a(clear) 자체 실패 — 아래 판부터
standoff 0.52   c 그대로 401
c 관절 되돌림    관절도 IK 실패 → 불연속이 아니라 transfer 가 안 풀린다
c+d 합침        0.243 rad, **아래 판부터** 깨짐 (d 의 되돌림이 손목을 구하고 있었다)
```

## 시뮬 없이 먼저 보는 도구

```bash
/tmp/usdenv/bin/python simulation/isaac/tools/reach_check.py <레벨.usdc>
```

레벨을 주면 **꽂을 수 있는 판 수**를 낸다. 지금 레벨은 2 개(0.4976 · 1.0417).
두 권을 다른 판에 꽂으려면 2 이상이어야 한다. **레벨을 고치면 판 띄우기 전에 이것부터.**
(`pxr`·`numpy` 가 시스템에 없어 가상환경이 필요하다. 없으면
`python3 -m venv /tmp/usdenv && /tmp/usdenv/bin/pip install usd-core numpy pyyaml`)

## 남은 것

- **위 판은 작업 공간 가장자리다.** 5/5 이지만 `pre_ins` 여유가 0.22 로 아래 판(0.65)의 셋째다.
  레벨이 바뀌면 `reach_check` 부터 다시 본다.
- `SIM_IK_AK=compare` 는 IK 를 두 번 풀어 **heartbeat 40 s 로도 계획이 안 끝난다.**
  판을 태우는 대조는 여기서 끝. 계획만 돌리는 오프라인 경로가 낫다.
- 거절(410)이 났을 때 FSM 이 전이하지 않고 멈추는 경로가 있다 (`shelving_system`, 이현민님).
  생중계에서 실패도 복귀도 아닌 상태가 제일 나쁘다.
