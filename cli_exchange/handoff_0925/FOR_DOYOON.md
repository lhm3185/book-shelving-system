# 도윤님이 하시면 더 쉬운 것 / 육안 확인이 필요한 것

## 0. 돌아오시면 띄울 명령 (네 권 풀 사이클)

```bash
cd ~/b1_arm
SIM_LEVEL=$HOME/levels/Final_Level_Library/Final_Level_RnD.usd \
SIM_SHELF_PRIM=/World/bookshelves_main/shelf_brown__book_shelf_11 \
SIM_SHELF_PRIMS="shelf_01=/World/bookshelves_main/shelf_brown__book_shelf_11;shelf_02=/World/bookshelves_main/shelf_brown__book_shelf_01" \
SIM_BOOK_IDS="['book_001','book_002','book_003','book_004']" \
SIM_RFID_TAGS="['rfid_001','rfid_002','rfid_003','rfid_004']" \
SIM_CLASS_CODES="['005.7','006.3','512.3','611.0']" \
SIM_SPINE_FROM_NEIGHBOURS=1 bash scripts/demo/full_cycle.sh
```

가까이서 녹화하려면 `REC_EYE="2.55 -4.15 1.55" REC_LOOK="2.62 -2.45 0.85" REC_EVERY=15` 을 더한다
(서가 A 기준. 기본 뷰는 방 전체라 로봇이 화면의 8% 다).

자체 진행 중 쌓이는 목록. 마지막 보고 때 같이 올린다.
**기준**: 우리가 코드로 하면 추측이 섞이는데 도윤님이 레벨·에셋을 고치거나
화면으로 보시면 한 번에 끝나는 것들.

---

## A. 레벨·에셋 수정으로 쉬워지는 것

### A-1. 서가 B 의 빈칸을 **서가 중심 쪽으로** (권장도 중)
- 지금: 아래 판 빈칸이 서가 중심에서 **+0.62 m** 떨어져 있다
- 그래서 꽂기 전 차체 옆이동이 0.624 m 로 예전 상한(0.600)을 넘었다
  → 코드로 우회했다(서가 폭 안의 이동이면 거리 상한을 안 본다, `a2d4755`)
- **중심 쪽(±0.45 m 안)으로 옮기시면** 그 예외가 필요 없어지고 A 와 같은 흐름이 된다
- 서가 A 는 빈칸이 −0.077 · +0.127 로 중심 근처라 한 번도 안 걸렸다

### A-2. 책 prim 하나에 **책 두 권**이 들어 있다 (권장도 높음)
- `decorative_book_set_..._cover15` 한 prim 안에 서가 양 끝 두 권(`Plane_087`, `Plane_88`)
- 그 둘을 감싸는 AABB 가 **623 mm** → 코드가 "623 mm 책 한 권" 으로 셌다
- 그 유령 덩어리가 서가 B 의 95.8 mm 빈칸을 덮어서, 우리가 **49.6 mm · 빈칸 1개**로
  두 번 잘못 보고했다 (도윤님이 "A 보다 넓다" 고 반박하셔서 잡았다)
- 코드는 잎 메시까지 내려가게 고쳤다(`fdc0b99`) — 지금은 맞게 센다
- **에셋에서 한 prim = 한 권으로 나눠 주시면** 이 종류의 착오가 구조적으로 사라진다
- 서가 A 에서는 그 덩어리가 빈칸 밖이라 우연히 안 걸렸다 — **9/9 는 그 운 위에 있었다**

### A-3. 서가 prim 이름이 A↔B 로 맞바뀌어 있다 (권장도 낮음, 기록용)
- 예전 레벨: `..._01` = 작업 서가(x +1.87~+3.28)
- 새 레벨:   `..._01` = 새 서가(x −1.37~+0.04), `..._11` 이 작업 서가
- 코드 기본값이 `..._01` 이라, 매번 `SIM_SHELF_PRIM` 을 줘야 한다
- 이름을 바꾸실 필요는 없다. 다만 **다음에 서가를 더 놓으실 때** 이름과 자리를
  같이 적어 주시면 우리가 좌표로 확인하는 수고가 준다

---

## B. 육안 확인이 우리 계측보다 정확한 것

### B-1. 꽂힌 책이 이웃보다 **튀어나오는가 / 들어가는가**
- 우리 계측: 계획이 이웃 앞끝보다 A 에서 27~38 mm, B 에서 48 mm **얕았다**
- `SIM_SPINE_FROM_NEIGHBOURS=1` 로 이웃 앞끝에 맞춰 꽂게 했다
- **화면에서 옆에서 보시면** 새 책이 이웃과 나란한지 한눈에 갈린다.
  숫자로는 "이웃 앞끝 중앙값" 인데, 이웃들 자체가 들쭉날쭉하면 중앙값이 답이 아닐 수 있다

### B-2. 트레이가 데크 위에 있는가 (도윤님이 지적하신 것)
- 우리 계측으로는 정상이다 — 앵커 대비 상대 위치가 9/9 판과 같고 회전만 반영된다
- 그런데 도윤님 눈에는 벗어나 보였다. **어느 시점이었는지**가 중요하다
  (이송 전 / 주행 중 / 주행 뒤). 주행 중이라면 추종이 밀린 것이라 다른 문제다
- 다음에 보이시면 **그 순간만 알려 주시면** 그 구간 로그를 바로 짚을 수 있다

### B-3. 팔이 서가·책을 스치는가 — **스냅샷으로 본다**
- 서가 책에 콜리전을 **일부러 안 넣으셨다** (2026-09-25 도윤님): 오차 범위를 넓게 가져가려는
  것이고, 넣으면 구현 범위가 넓어지고 까다로운 예외가 생긴다. 시간 여건상 이게 최선이다.
  → **우리가 코드로 충돌을 잡을 길은 없다. 스냅샷 육안 확인이 유일한 검사다.**
- 서가 **본체**는 A·B 둘 다 콜리전이 있다 (13:18 레벨에서 B 도 추가됨) — 팔이 서가를
  뚫는 것은 물리가 막아 준다. 못 막는 것은 **책**이다.
- 그래서 판을 돌릴 때 `REC_EVERY=30`(2 fps)으로 뷰포트를 찍는다. 볼 구간:
  · 파지 직후 트레이에서 빼는 순간
  · 서가 앞에서 손목을 크게 돌리는 구간 (위 판 되돌림)
  · 꽂아 넣는 순간과 손을 떼는 순간
- 프레임은 `logs/rec/` 에 쌓인다. 한 판이 GB 단위라 확인 뒤 지운다.

---

## C. 우리가 손대지 않기로 한 것 (도윤님 결정 필요)

### C-1. FSM 이 `shelf_id` 를 안 채운다 — 이현민님 패키지
- `DetectTargetSlot` 에 `job_id`·`book_id` 만 온다
- 지금은 "서 있는 자리에서 가장 가까운 서가" 로 우회 (`1643f31`)
- 네 권까지는 우회로 버티지만, 근본은 FSM 이 채우는 것

### C-2. `410` 거절 뒤 FSM 이 전이하지 않고 멈춘다 — 이현민님 패키지
- 어제 17분 서 있었다. **생중계에서 실패도 복귀도 아닌 상태가 제일 나쁘다**
- 한 권이라도 넣고 복귀하는 게 낫다

### C-3. `nav_manager` 패치가 커밋 없이 작업 트리에만 있다
- 도윤님 지시대로 커밋 안 함 (잘 되면 나중에 팀 공유)
- 두 PC 가 같은 내용으로 맞춰 둠. 지금 `+36 -2`

### A-4. 트레이 검출 ROI 가 앞 두 칸만 덮는다 (권장도 높음 — **네 권의 관문**)
- `perception.yaml` 의 `book_roi_max` y 상한이 **+0.30**
- 트레이 칸 실측: 칸0 −0.188 · 칸1 −0.004 · 칸2 **+0.180**(가장자리) · 칸3 **+0.364**(밖) · 칸4 +0.548(밖)
- 그래서 **세 권째부터 못 찾는다** — 네 권 예행이 3권째 `411 NOT_READY` 로 멈췄다
- 두 권까지는 칸0·칸1 이라 여태 한 번도 안 걸렸다
- 코드로는 y 상한을 0.65 로 넓히면 된다(비전팀 설정이라 상의 중).
  **도윤님 쪽 대안**: 트레이에서 책을 **앞 두 칸에만** 놓게 배치하시면 설정을 안 건드려도 된다.
  다만 네 권이면 칸이 모자라니, ROI 를 넓히는 쪽이 맞을 것 같다

### A-5. 녹화 카메라를 작업 자리에 하나 놓아 주시면 (권장도 중)
- 기본 녹화 뷰가 방 전체라 로봇이 **화면의 8%** — 팔이 책을 스치는지 판별이 안 된다
- 지금은 `REC_EYE`/`REC_LOOK` 으로 그때그때 옮겨 쓰고 있다(`c99a137`)
- **레벨에 서가 A·B 작업 자리를 보는 카메라 prim 두 개**를 놓아 주시면 매번 좌표를 안 줘도 된다
- 서가 책에 콜리전이 없는 설계이므로, 이 그림이 **유일한 충돌 검사**다

### A-6. 위 판 오른쪽 책 하나가 **원래 기울어 있다** (권장도 낮음 — 보기 문제)
- 서가 A 위 판(thirdFloor) 오른쪽에 비스듬히 선 책이 하나 있다
- **판 시작 프레임(f000010)부터** 그 상태다 — 우리가 건드려서 생긴 게 아니라 레벨 원본
- 기능에는 지장이 없다(빈칸 계산은 그 책도 세운 폭으로 잡는다). 다만 **시연 화면에
  그대로 나온다** — 근접 녹화에서 눈에 띈다. 세워 두시면 그림이 깔끔하다

---

## D. 2026-09-25 오후 — 서가별로 묶어 도는 것 (도윤님 지시 반영)

### D-1. 무엇을 고쳤나
지시: *"책장 B 두 권 반납 후에 A까지 바로 주행하고 남은 두 권 삽입 후 홈위치 복귀"*

원인은 두 개였고 둘 다 코드 쪽이었다 (`cli_exchange/handoff_0925/ROUTE_GROUPING.md` 에 실측 포함):

1. **책 순서**는 코드가 정하는 게 아니라 넣어 준 순서 그대로다 (`job_planner` 가 `zip` 만 한다)
   → 환경변수 한 줄로 B 두 권을 앞으로 뺐다
2. **매번 주행**한 건 "이미 앞에 서 있으면 생략" 판정이 서가 A 경로에만 안 걸려 있어서다
   (`route is not self.patrol_route` 가드). 그 가드는 지금 불필요해서 뺐다 —
   `library_loop` 는 마지막 점이 `patrol_start` 로 되돌아와 A 작업 자리와 같다
3. **B → A** 를 곧장 가면 서가 5개를 관통한다(실측). 통로로 도는 경로를 새로 넣었다

### D-2. 거리 (레벨 격자에서 실측, 카트 반폭 0.45 m)

    B → A 곧장                  5.25 m   서가 5개 관통   ✗
    B → 통로 → A (새 경로)     10.26 m   관통 0          ✓
    지금까지 (library_loop)     37.28 m   관통 9회        ✗

주행 횟수 4번 → **3번**. 그리고 B 를 먼저 도니까 마지막 복귀가 9/9 로 검증된 A→홈 경로가 된다.

### D-3. 도윤님이 봐 주시면 좋은 것 (B-4)
- **B → A 로 옮겨 가는 통로 주행**이 화면에서 어떻게 보이는지. 격자 계산으로는 깨끗하지만,
  카트가 통로를 도는 그림 자체는 아직 눈으로 본 적이 없다
- 통로 폭은 y −0.15 ~ +1.67 (1.82 m), 카트 반폭 0.45 라 여유는 충분하다는 계산이다

### D-4. 오늘 오후에 헛돈 것 두 가지 (같은 실수 반복 방지용 기록)
- 14:37 판은 **조작 노드가 아예 안 떴다.** `MAN_EXTRA` 에 넣은 리스트에 공백이 있어
  ROS 인자 파서가 `-p vision_grasp_region_max:=[-0.15,` 에서 끊었다. FSM 은 없는 서버를
  12분 기다렸다. → 그 값은 랩탑이 `manipulation.yaml` 에 상수로 넣어 둬서 이제 필요 없다
- `full_cycle.sh` 의 정리는 **Isaac 과 rqt 만** 끈다. 런처 셸(`full_cycle.sh`) 자체는 안 끈다 —
  한때 셸이 3개 겹쳐 돌았다. 다음 판 띄우기 전에 `ps | grep full_cycle` 로 확인할 것

---

## D-5. **네 권 완주 성공** (2026-09-25 15:34 · 15:49 두 판 연속)

`bash scripts/demo/full_cycle.sh` 에 아래 환경변수만 주면 그대로 재현된다
(검출 신뢰도·어긋남 기준은 이제 스크립트 기본값이라 안 줘도 된다):

    export SIM_LEVEL=$HOME/levels/Final_Level_Library/Final_Level_RnD.usd
    export SIM_SHELF_PRIM=/World/bookshelves_main/shelf_brown__book_shelf_11
    export SIM_SHELF_PRIMS="shelf_01=/World/bookshelves_main/shelf_brown__book_shelf_11;shelf_02=/World/bookshelves_main/shelf_brown__book_shelf_01"
    export SIM_SPINE_FROM_NEIGHBOURS=1
    export SIM_BOOK_IDS="['book_003','book_004','book_001','book_002']"
    export SIM_RFID_TAGS="['rfid_003','rfid_004','rfid_001','rfid_002']"
    export SIM_CLASS_CODES="['512.3','611.0','005.7','006.3']"

### 결과 (두 판)

    권  서가      중심오차(15:34 / 15:49)   기울기    양옆 여유(15:49)
    1   B 위판     -1.8 / -1.8 mm           +0.32°    27.0 / 32.5 mm
    2   B 아래판   -3.7 / -3.9 mm           +0.17°    13.6 / 23.1 mm
    3   A 아래판   +0.0 / +1.1 mm           +0.34°    10.6 / 10.2 mm
    4   A 위판     -1.1 / -3.5 mm           +0.21°     0.8 /  9.6 mm   ← A-7 참고

겹침 0건 (그 판의 서가 책 41~44권과 대조). 홈 복귀 좌표·각도 두 판 모두 정확.
한 권당 1분 50초, 전체 약 10분.

### 가는 길에 막혔던 세 가지 — 전부 **두 서가를 실제로 써야 드러나는** 것들이었다

1. **유령 빈칸** (`410`): 우리가 꽂은 책이 서가 상자 필터를 안 탔다. B 는 A 의 복사본이라
   판 높이가 같아, B 에 꽂은 두 권이 A 를 잴 때 목록에 섞였다 → 3 m 짜리 빈칸 →
   네 권째가 차체를 1.454 m 옮기려다 멈췄다. 필터 한 줄 추가 (`book_scene.py`)
2. **트레이 검출 여유 없음** (`411`): 30 s 창에 트리거 60회, 관측 1개. 성공하던 판도 2개가
   전부라 아슬아슬했다. 신뢰도 0.75 → **0.55**. ROI·안전범위·합의가 뒤를 받친다
3. **`406` 이 허위**: 책 원점↔형상중심 지렛대가 **109.6~176.7 cm** 라 회전 1.5° 가 3.4 cm 로
   찍혔다. 키네마틱 파지는 매 스텝 책을 손에 다시 써 넣으므로 그 기준은 애초에 미끄러짐을
   못 본다 → 쥔점 기준(`SIM_DRIFT_METRIC=grip`)으로 전환

---

### A-7. ~~서가 A 위 판 빈칸을 더 벌려 주시면~~ — **철회. 레벨 문제가 아니었다**

도윤님 지적(2026-09-25): *"A책장은 기존에 삽입 모두 성공했던 케이스였는데 왜 갑자기
빈칸을 늘려달라고 요청한거야?"* — 맞는 지적이고, 내가 잘못 짚었다.

**빈칸은 그대로다. 거기 들어가는 책이 바뀌었다.** 같은 칸(이웃이 `Plane_110`/`Plane_112`
로 동일)에서:

    2권 판 (기존)   규격 두께 35.2 mm → 좌 8.4 · 우 10.9 mm  (합 19.3)
    4권 판 (지금)   규격 두께 44.3 mm → 좌 0.8 · 우  9.6 mm  (합 10.4)

두께 차 9.1 mm, 여유 감소 8.9 mm — 전부 책 두께로 설명된다. 빈칸은 56 mm 그대로다.

진짜 문제는 **네 권의 칸 배정이 거꾸로**라는 것이다:

    B 위판   81 mm  ←  35.2 mm   여유 45.8   ← 제일 넓은 칸에 제일 얇은 책
    B 아래판 96 mm  ←  43.1 mm   여유 52.9
    A 아래판 60 mm  ←  38.2 mm   여유 21.8
    A 위판   56 mm  ←  44.3 mm   여유 11.7   ← 제일 좁은 칸에 제일 두꺼운 책

두꺼운 책을 넓은 칸으로 보내면 최소 여유가 11.7 → 20.8 mm 로 거의 두 배가 된다.
**레벨을 안 건드리고 되는 일이다.**

아직 확인 못 한 것: 칸 선택은 `choose_gap` 이 "겨누던 x 에 **가장 가까운**, 들어가는
빈칸" 으로 고르지 두께로 고르지 않는다. 서가는 분류기호로 정해지고, 첫 A 책이 들어갈
때는 두 칸이 다 비어 있다 — 책 순서만 바꿔서 원하는 칸으로 갈지는 **돌려봐야 안다.**

### B-5. 서가 B 근접 녹화 — **확인 완료** (2026-09-25 16:47 판, `logs/rec/0925_164757/`)

삽입 전(f000300, 16:50:31)과 후(f001060, 16:58:00)를 잘라 비교했다.

- **아래판**: 붉은 `PEEPING TIM` 이 우리가 꽂은 책. 똑바로 서 있고 책등 선이 이웃과 맞다.
  양옆 어디에도 안 닿는다 (계측 좌 14.0 / 우 22.7 mm 와 일치)
- **위판**: 왼쪽 무리와 가운데 무리 사이에 흰 책이 새로 섰다. 마찬가지로 곧고 나란하다
- **이웃을 민 흔적이 없다.** 좌우 책 무리가 삽입 전과 위치·자세 그대로다

서가 책에 콜리전이 없는 설계라 이 그림이 유일한 물리 검사였다 — 계측(겹침 0건)과 일치한다.

**덧**: 서가 B 위판에도 **원래 비스듬히 선 책**이 하나 있다(노란 책). 삽입 전 프레임부터
그 상태다 — A-6 과 같은 것으로, B 가 A 복사본이라 따라왔다. 세워 두시면 두 서가가 같이 깔끔해진다.

---

## E. 레벨 수정 반영 확인 (2026-09-25 16:47)

도윤님이 서가 A 의 빈칸 옆 책을 한 권씩 지워 주신 뒤 실측:

    서가·판        이전     지금
    A 위판        56 mm  → 82.9 mm
    A 아래판      60 mm  → 85.9 mm
    B 위판        81 mm    80.6 mm
    B 아래판      96 mm    95.8 mm

런타임 스캔도 같은 값을 잡았다 (`83mm@+0.139`, `86mm@-0.090`). 네 칸이 80.6~95.8 mm 로
고르다. 제일 두꺼운 책이 44.3 mm 라 **최악의 배정에서도 한쪽 18 mm** 가 남는다 —
A-7 에서 걱정한 배정 문제는 사라졌다.

### 네 권 완주 3/3 (15:34 · 15:49 · 16:47)

    권  서가      중심오차   좌 여유   우 여유    (16:47 판, 넓힌 레벨)
    1   B 위판    -2.5 mm   26.5 mm   33.1 mm
    2   B 아래판  -3.4 mm   14.0 mm   22.7 mm
    3   A 아래판  +0.4 mm   22.9 mm   24.0 mm    (넓히기 전 10.6 / 10.2)
    4   A 위판    -3.5 mm   14.5 mm   23.3 mm    (넓히기 전  0.8 /  9.6)

최소 여유 14.0 mm. 겹침 0건. 주행 판정 네 번 모두 의도대로. 홈 복귀 좌표·각도 정확.
